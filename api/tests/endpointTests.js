var chai = require('chai');

describe("Test endpoints", () => {
	
	it("Test valid payslip returned when valid employee csv data supplied", () => {
		
	});

	it("Test valid payslip returned when valid employee json data supplied", () => {
		
	});
	
	it("Test 400 error returned with validation error when invalid employee csv data supplied", () => {
		
	});	
	
	it("Test 400 error returned with validation error when invalid employee json data supplied", () => {
		
	});	
});